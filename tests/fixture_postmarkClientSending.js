var mocha = require('mocha');
var assert = require('assert');

describe('client', function(done){
	describe('send', function(done){
		/*
		it('should return the message status.', function(done){
			//assert.notEqual(null,null)
			done();
		});
		*/
	});
});